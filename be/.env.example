# Backend will read one of:
# - `MONGO_URI` (preferred)
# - `MONGO_DB_SERVER` (supported)
# - `NEXT_PUBLIC_MONGO_DB_SERVER` (fallback)
MONGO_URI=mongodb+srv://USER:PASS@HOST/DB?retryWrites=true&w=majority&appName=APP

JWT_SECRET=<your-jwt-secret>

# Frontend origin yang diizinkan (wajib kalau pakai cookie + CORS)
# Contoh untuk Next dev:
# CORS_ORIGIN=http://localhost:3000
CORS_ORIGIN=